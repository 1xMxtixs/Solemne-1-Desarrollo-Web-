<div class="space-y-6">
  <h2 class="text-2xl font-bold text-primary">Crear / Editar Producto</h2>
  <p class="text-secondary text-sm mb-4">
    Completa los datos para agregar o modificar un producto del catálogo.
  </p>

  <form class="space-y-6">
    <!-- Datos básicos -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div>
        <label class="block text-sm text-primary mb-1">Nombre del producto</label>
        <input type="text" class="input" placeholder="Ej: Spaghetti Clásico" />
      </div>
      <div>
        <label class="block text-sm text-primary mb-1">Categoría</label>
        <select class="input">
          <option value="">Selecciona una categoría</option>
          <option>Pastas</option>
          <option>Salsas</option>
          <option>Bebidas</option>
        </select>
      </div>
    </div>

    <!-- Descripción -->
    <div>
      <label class="block text-sm text-primary mb-1">Descripción</label>
      <textarea class="input h-24" placeholder="Escribe una breve descripción del producto"></textarea>
    </div>

    <!-- Precio y stock -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div>
        <label class="block text-sm text-primary mb-1">Precio</label>
        <input type="number" class="input" placeholder="$0" />
      </div>
      <div>
        <label class="block text-sm text-primary mb-1">Stock disponible</label>
        <input type="number" class="input" placeholder="Ej: 100" />
      </div>
    </div>

    <!-- Imagen -->
    <div>
      <label class="block text-sm text-primary mb-1">Imagen del producto</label>
      <input type="file" class="input" />
      <p class="text-xs text-secondary mt-1">
        Formatos permitidos: JPG, PNG. Máx: 2MB.
      </p>
    </div>

    <!-- Variantes -->
    <div>
      <label class="block text-sm text-primary mb-1">Variantes</label>
      <input type="text" class="input mb-2" placeholder="Ej: 250g, 500g, 1kg" />
      <button
        type="button"
        class="px-4 py-2 bg-accent text-white rounded-lg hover:bg-accent-secondary transition"
      >
        + Agregar Variante
      </button>
    </div>

    <!-- Acciones -->
    <div class="flex justify-end gap-4">
      <button
        type="button"
        id="cancelar-modal"
        class="px-4 py-2 bg-gray-200 rounded-lg hover:bg-gray-300 transition"
      >
        Cancelar
      </button>
      <button
        type="submit"
        class="px-6 py-2 bg-accent text-white rounded-lg hover:bg-accent-secondary shadow-sm"
      >
        Guardar Producto
      </button>
    </div>
  </form>
</div>
